services:
  - type: web
    name: code-analyzer
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: OPENROUTER_API_KEY
        sync: false